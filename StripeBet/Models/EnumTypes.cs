namespace StripeBet.Models
{
    public enum TransactionType
    {
        Deposit,
        Withdrawal
    }

    public enum BetOutcome
    {
        Win,
        Loss
    }

    public enum BetColor
    {
        Black,
        Red

    }
}
